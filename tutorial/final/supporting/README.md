# Supporting files from the completed run

These are the calculator files copied into the completed run folder.

- `ase_command.py`: calculator used for generation force checks.
- `aims_command.py`: FHI-aims calculator used for labels.

They are kept here so the completed run records which calculator scripts were
used.
